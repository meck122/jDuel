// Music is hidden pending licensing review.
// Set to true (and ensure mp3 files are present in frontend/src/assets/music/)
// then rebuild to enable the feature. See scripts/upload-music.sh for VPS uploads.
export const MUSIC_ENABLED = false;
